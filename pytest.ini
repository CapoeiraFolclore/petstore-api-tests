[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    smoke: quick sanity checks against the target API
    pet: pet resource endpoints
    store: store resource endpoints
    user: user resource endpoints
